import { types } from "../../../types/types";

export const horarioReducer = (state={},action) => {
    switch (actio.type) {
        case types.AgregarDiaHoraHorario:
            return{

            }
            
    
        default:
            return state;
    }
}
